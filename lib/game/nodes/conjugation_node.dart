import 'package:evo/game/node_types/output_node.dart';

class ConjugationNode extends OutputNode {
  ConjugationNode()
      : super("Conjugation", "Bacteria of the same species can exchange genes.",
            2);
}
